import React from 'react';

const Header = () => {
  return (
    <nav className="navbar navbar-dark bg-primary">
      <span className="navbar-brand">小口現金システム</span>
    </nav>
  );
};

export default Header;
